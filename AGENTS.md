# REIT Assistant — Agent instructions

## Workspace scope (mandatory)

All file operations, reads, writes, and terminal work are **restricted to this directory only**:

`/Users/nancyfletcher/Documents/reit-assistant-fqe`

Do not access system folders, user home paths outside this project, or unrelated repositories.

See `.cursor/rules/workspace-scope.mdc`.

## Beta repo layout

Keep only: `app/`, `components/`, `services/`, `hooks/`, `constants/`, `types/`, `utils/`, `assets/`, `docs/`, and root config files. See `docs/BETA_REPO_STRUCTURE.md`.

## Expo

Read https://docs.expo.dev/versions/v54.0.0/ before writing Expo-related code.

## Design reference

Kimi AI specs: `docs/01_Style_Guide.md` through `docs/07_Success_Criteria_Validation.md`
